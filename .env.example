# 复制为 .env 并填入真实值（.env 已被 .gitignore 忽略，不会提交到仓库）
PORT=3000

# 会话密钥：请改成一个足够长的随机字符串（至少 32 字符）
SESSION_SECRET=change_me_to_random_string

# 每个账号一个密码变量，用户名全大写。未配置的用户无法登录。
USER_PASSWORD_ADMIN=change_me_admin_password
USER_PASSWORD_TOM=change_me_tom_password
