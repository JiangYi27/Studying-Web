# 树形结构

## 二叉树的基本术语

在讨论二叉树前，先明确几个常用术语：

- **根节点（Root）**：树中最上层的节点，一棵树有且仅有一个根节点
- **父节点与子节点**：一个节点向下直接相连的节点称为其子节点，反之该节点为父节点
- **叶子节点（Leaf）**：没有子节点的节点
- **子树（Subtree）**：节点及其所有后代构成的树
- **节点的度（Degree）**：一个节点拥有的子树个数
- **树的深度/高度（Depth/Height）**：从根节点到最远叶子节点的层数，根节点为第1层
- **满二叉树（Full Binary Tree）**：每一层的节点数都达到最大值
- **完全二叉树（Complete Binary Tree）**：除最后一层外每层都是满的，且最后一层的节点都靠左排列（堆通常用完全二叉树存储）
- **二叉搜索树（BST）**：满足"左子树所有节点 < 根节点 < 右子树所有节点"的二叉树

其中**满二叉树**和**完全二叉树**是判断二叉树是否为"完全"形态的重要概念，也是堆、哈夫曼树等结构的基础。

## 二叉树（链式存储）

### 基本结构

```c
typedef struct TreeNode {
    int data;
    struct TreeNode *left;
    struct TreeNode *right;
} TreeNode;
```

### 创建节点

```c
TreeNode *create_node(int data) {
    TreeNode *node = (TreeNode *)malloc(sizeof(TreeNode));
    if (node != NULL) {
        node->data = data;
        node->left = NULL;
        node->right = NULL;
    }
    return node;
}
```

### 二叉树遍历

#### 前序遍历（根-左-右）

```c
void preorder(TreeNode *root) {
    if (root == NULL) {
        return;
    }
    printf("%d ", root->data);
    preorder(root->left);
    preorder(root->right);
}
```

#### 中序遍历（左-根-右）

```c
void inorder(TreeNode *root) {
    if (root == NULL) {
        return;
    }
    inorder(root->left);
    printf("%d ", root->data);
    inorder(root->right);
}
```

#### 后序遍历（左-右-根）

```c
void postorder(TreeNode *root) {
    if (root == NULL) {
        return;
    }
    postorder(root->left);
    postorder(root->right);
    printf("%d ", root->data);
}
```

#### 层序遍历（广度优先）

```c
#include <stdio.h>
#include <stdlib.h>

#define MAX_QUEUE_SIZE 100

void level_order(TreeNode *root) {
    if (root == NULL) {
        return;
    }

    TreeNode *queue[MAX_QUEUE_SIZE];
    int front = 0, rear = 0;

    queue[rear++] = root;

    while (front < rear) {
        TreeNode *current = queue[front++];
        printf("%d ", current->data);

        if (current->left != NULL) {
            queue[rear++] = current->left;
        }
        if (current->right != NULL) {
            queue[rear++] = current->right;
        }
    }
}
```

## 二叉搜索树（BST）

### 基本性质

- 左子树的所有节点值小于根节点值
- 右子树的所有节点值大于根节点值
- 左右子树也分别是二叉搜索树

### 插入操作

```c
TreeNode *bst_insert(TreeNode *root, int data) {
    if (root == NULL) {
        return create_node(data);
    }

    if (data < root->data) {
        root->left = bst_insert(root->left, data);
    } else if (data > root->data) {
        root->right = bst_insert(root->right, data);
    }

    return root;
}
```

### 查找操作

```c
TreeNode *bst_search(TreeNode *root, int data) {
    if (root == NULL || root->data == data) {
        return root;
    }

    if (data < root->data) {
        return bst_search(root->left, data);
    } else {
        return bst_search(root->right, data);
    }
}
```

### 删除操作

```c
TreeNode *bst_min(TreeNode *node) {
    while (node->left != NULL) {
        node = node->left;
    }
    return node;
}

TreeNode *bst_delete(TreeNode *root, int data) {
    if (root == NULL) {
        return NULL;
    }

    if (data < root->data) {
        root->left = bst_delete(root->left, data);
    } else if (data > root->data) {
        root->right = bst_delete(root->right, data);
    } else {
        // 找到要删除的节点
        if (root->left == NULL) {
            TreeNode *temp = root->right;
            free(root);
            return temp;
        } else if (root->right == NULL) {
            TreeNode *temp = root->left;
            free(root);
            return temp;
        }

        // 有两个子节点：用右子树的最小值替换
        TreeNode *temp = bst_min(root->right);
        root->data = temp->data;
        root->right = bst_delete(root->right, temp->data);
    }

    return root;
}
```

## 堆（优先队列）

### 基本概念

堆是一种完全二叉树，分为最大堆和最小堆。

- **最大堆**：每个节点的值大于等于其子节点的值
- **最小堆**：每个节点的值小于等于其子节点的值

### 基于数组的堆

```c
typedef struct {
    int *data;
    int size;
    int capacity;
} MinHeap;

MinHeap *create_heap(int capacity) {
    MinHeap *heap = (MinHeap *)malloc(sizeof(MinHeap));
    if (heap == NULL) {
        return NULL;
    }

    heap->data = (int *)malloc(capacity * sizeof(int));
    if (heap->data == NULL) {
        free(heap);
        return NULL;
    }

    heap->size = 0;
    heap->capacity = capacity;
    return heap;
}

void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

void heapify_up(MinHeap *heap, int index) {
    while (index > 0) {
        int parent = (index - 1) / 2;
        if (heap->data[index] < heap->data[parent]) {
            swap(&heap->data[index], &heap->data[parent]);
            index = parent;
        } else {
            break;
        }
    }
}

void heapify_down(MinHeap *heap, int index) {
    while (1) {
        int left = 2 * index + 1;
        int right = 2 * index + 2;
        int smallest = index;

        if (left < heap->size && heap->data[left] < heap->data[smallest]) {
            smallest = left;
        }
        if (right < heap->size && heap->data[right] < heap->data[smallest]) {
            smallest = right;
        }

        if (smallest != index) {
            swap(&heap->data[index], &heap->data[smallest]);
            index = smallest;
        } else {
            break;
        }
    }
}

int heap_push(MinHeap *heap, int value) {
    if (heap->size >= heap->capacity) {
        return -1;  // 堆满
    }

    heap->data[heap->size] = value;
    heapify_up(heap, heap->size);
    heap->size++;
    return 0;
}

int heap_pop(MinHeap *heap, int *value) {
    if (heap->size <= 0) {
        return -1;  // 堆空
    }

    *value = heap->data[0];
    heap->size--;
    heap->data[0] = heap->data[heap->size];
    heapify_down(heap, 0);
    return 0;
}

int heap_peek(MinHeap *heap, int *value) {
    if (heap->size <= 0) {
        return -1;
    }
    *value = heap->data[0];
    return 0;
}

void destroy_heap(MinHeap *heap) {
    if (heap != NULL) {
        free(heap->data);
        free(heap);
    }
}
```

### 堆排序

```c
void heap_sort(int arr[], int n) {
    // 构建最大堆
    for (int i = n / 2 - 1; i >= 0; i--) {
        // 下滤操作
        int parent = i;
        while (1) {
            int left = 2 * parent + 1;
            int right = 2 * parent + 2;
            int largest = parent;

            if (left < n && arr[left] > arr[largest]) {
                largest = left;
            }
            if (right < n && arr[right] > arr[largest]) {
                largest = right;
            }

            if (largest != parent) {
                int temp = arr[parent];
                arr[parent] = arr[largest];
                arr[largest] = temp;
                parent = largest;
            } else {
                break;
            }
        }
    }

    // 逐个提取最大元素
    for (int i = n - 1; i > 0; i--) {
        int temp = arr[0];
        arr[0] = arr[i];
        arr[i] = temp;

        // 重新堆化
        int parent = 0;
        while (1) {
            int left = 2 * parent + 1;
            int right = 2 * parent + 2;
            int largest = parent;

            if (left < i && arr[left] > arr[largest]) {
                largest = left;
            }
            if (right < i && arr[right] > arr[largest]) {
                largest = right;
            }

            if (largest != parent) {
                int temp = arr[parent];
                arr[parent] = arr[largest];
                arr[largest] = temp;
                parent = largest;
            } else {
                break;
            }
        }
    }
}
```

## 树形结构对比

| 结构 | 查找 | 插入 | 删除 | 空间 |
|------|------|------|------|------|
| 二叉搜索树 | O(log n) 平均 | O(log n) 平均 | O(log n) 平均 | O(n) |
| 二叉搜索树（最坏） | O(n) | O(n) | O(n) | O(n) |
| 堆（优先队列） | O(1) | O(log n) | O(log n) | O(n) |

## 二叉树的应用

1. **表达式解析**：表达式树
2. **文件系统**：目录树
3. **数据库索引**：B 树、B+ 树
4. **哈夫曼编码**：压缩算法
5. **语法分析**：编译器