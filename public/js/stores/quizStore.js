/**
 * quizStore —— 答题数据 store（预留扩展）
 * 当前 quizStats 保留在 studyStore 中，
 * 如后续需要独立管理答题历史，可在此扩展。
 */
'use strict';

// 当前为占位模块，实际数据仍在 studyStore.quizStats
// 如需独立：
//   const quizStore = window.quizStore = { history: [], ... }

module.exports = {};
